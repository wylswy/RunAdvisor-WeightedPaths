# Maven
target/
/com/
dependency-reduced-pom.xml
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
release.properties

# IDE
.idea/
*.iml
.vscode/
*.classpath
.project
.settings/
bin/

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Logs / local runs
*.log
RunAdvisorLogs/
# 游戏运行时日志（每次运行生成，不进 git）
sendToDevs/

# Build / deploy artifacts (jar built locally)
*.jar
!lib/*.jar
# 游戏本体 jar：版权原因不进仓，用 scripts/setup-libs.ps1 从游戏安装目录获取
lib/desktop-1.0.jar

# Temp / scratch (not source)
hr_main.js
hr_silent.html
help_pom.txt

# Optional: keep Cursor rules in repo (project conventions)
# .cursor/ is included intentionally

# Runtime cache
**/__pycache__/
*.pyc

# Scratch fetch data (temp, not project source)
研究草稿/

# Experimental AI-setup scripts (local convenience, not project source)
scripts/codex-deepseek-setup-en.ps1
scripts/启动Claude-DeepSeek.bat
scripts/启动Codex-DeepSeek.bat
scripts/启动Hermes-DeepSeek.bat
scripts/配置Codex-DeepSeek.bat
run_tmp_test.bat
