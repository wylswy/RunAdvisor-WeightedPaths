# Maven
target/
/com/
dependency-reduced-pom.xml
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
release.properties

# IDE
.idea/
*.iml
.vscode/
*.classpath
.project
.settings/
bin/

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Logs / local runs
*.log
RunAdvisorLogs/
# 游戏运行时日志（每次运行生成，不进 git）
sendToDevs/

# Build / deploy artifacts (jar built locally)
*.jar
!lib/*.jar

# Temp / scratch (not source)
hr_main.js
hr_silent.html
help_pom.txt

# Optional: keep Cursor rules in repo (project conventions)
# .cursor/ is included intentionally
